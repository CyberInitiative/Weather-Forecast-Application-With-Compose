package com.example.weathercompose.data.mapper

/*
fun LocationEntity.mapToWidgetLocation(): WidgetLocationDataModel {
    return WidgetLocationDataModel(
        locationId = locationId,
        locationName = name,
        timeZone = timeZone,
    )
}

fun WidgetLocationWithForecasts.mapToWidgetLocation(): WidgetLocation {
    return WidgetLocation(
        locationId = locationId,
        locationName = locationName,
        dailyMaxTemperature = dailyMaxTemperature,
        dailyMinTemperature = dailyMinTemperature,
        weatherDescription = weatherDescription
    )
}

fun HourlyForecastEntity.mapToWidgetHourlyForecast(): WidgetHourlyForecast {
    return WidgetHourlyForecast(
        time = LocalTime.parse(time),
        temperature = temperature,
        weatherDescription = weatherDescription,
        isDay = isDay,
    )
}*/